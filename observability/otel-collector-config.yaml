receivers:
  otlp:
    protocols:
      grpc: {}
      http: {}

exporters:
  logging:
    logLevel: info
  otlp/tempo:
    endpoint: tempo:4317
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp/tempo, logging]
